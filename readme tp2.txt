-se instancia el observer.
-se instancia el observable.
-se agrega el observer al observable.
-se cambian los valores de los atributos del observable.
-dentro de los metodos que cambian los atributos del observable.
-se notifica al observer.
-el observer muestra los cambios hechos en el observable.

-se le pasa una instancia actual del observable y el valor anterior del atributo que cambio.